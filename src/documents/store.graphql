query SearchStores($search: String) {
  stores(search: $search) {
    id
    name
    banner {
      imageUrl
    }
  }
}

query Store($id: ID!) {
  store(id: $id) {
    id
    name
    banner {
      imageUrl
    }
  }
}
